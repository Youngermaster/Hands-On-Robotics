// Copy to `secrets.h` (same directory) and fill in. `secrets.h` is gitignored.
#pragma once

#define WIFI_SSID     "your-2.4ghz-ssid"
#define WIFI_PASSWORD "your-wifi-password"

// Optional: pick a fixed port for the WS server. 81 is convention to
// avoid clashing with anything that already runs on 80.
#define WS_PORT       81
