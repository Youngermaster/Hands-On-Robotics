// Copy this file to `secrets.h` (same directory) and fill in your values.
// `secrets.h` is git-ignored — do NOT commit it.
#pragma once

#define WIFI_SSID     "your-2.4ghz-ssid"
#define WIFI_PASSWORD "your-wifi-password"

// Full URL of the Axum endpoint. Use your laptop's LAN IP (not localhost).
// macOS:  ipconfig getifaddr en0
// Linux:  hostname -I
#define SERVER_URL    "http://192.168.1.42:8080/telemetry"
